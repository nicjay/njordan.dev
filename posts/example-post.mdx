---
title: Example Post
description: This frontmatter description will appear below the title
image: /images/example-post/title1.jpg
---

This is an example post, with a [link](https://nextjs.org) and a React component:

The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

Go back [home](/).

![The San Juan Mountains are beautiful!](/images/example-post/placeholder1.jpg 'San Juan Mountains')

This is an example post, with a [link](https://nextjs.org) and a React component:

The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

This is an example post, with a [link](https://nextjs.org) and a React component:

The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.
This is an example post, with a [link](https://nextjs.org) and a React component:

The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

This is an example post, with a [link](https://nextjs.org) and a React component:
The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

This is an example post, with a [link](https://nextjs.org) and a React component:
The title and description are pulled from the MDX file and processed using `next-mdx-remote`. Additionally, links are rendered using a custom component passed to `next-mdx-remote`.

![The San Juan Mountains are beautiful!](/images/example-post/placeholder1.jpg 'San Juan Mountains')
